{
  "name": "cdass-enroll",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-only helper for filling Colorado CDASS / PPL attendant enrollment paperwork.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "node scripts/setup-assets.mjs",
    "postinstall": "node scripts/setup-assets.mjs"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "tesseract.js": "^6.0.1",
    "zxing-wasm": "^2.1.2"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
